# Implementation Plans

Generated by the improve skill on 2026-06-21. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add subagent tool-discovery mechanism to replace the fragile mapping | P2 | S | — | DONE |
| 002  | Make update-setup portable across Windows, macOS, and Linux | P3 | S | — | DONE |
| 003  | Add tsconfig.json for extension type-checking and editor IntelliSense | P2 | S | — | DONE |
| 004  | Add `ask_user_question` to subagents and post-hoc file diffing for worker results | P2 | S | 001 | SUPERSEDED (by 005) |
| 005  | Integrate subagent file changes with filechanges TUI widget | P2 | M | — | DONE |
| 006  | Enable `ask_user_question` in headless subagents via temp-file relay | P2 | L | 005 | DONE |
| 007  | Clean up dead code, duplicates, and truncLine bug in subagents extension | P2 | S | — | DONE |

## Dependency notes

- 004 is superseded because approach 2 (filechanges TUI integration) provides the visual feedback the user needs.
- 004's `ask_user_question` addition (CUSTOM_TOOL_EXTENSIONS + agent tools lists) is already ported to `advisor/005-filechanges-integration` which is the base for plan 006.
- 006 depends on 005 because it uses the same `advisor/006-subagent-ask-user` branch (forked from `advisor/005-filechanges-integration`).
Plans 001-003 are independent.

## Findings considered and rejected

- API key exposed in 3 local files: properly gitignored; OS keychain integration is overkill for a single-user machine.
- Mixed `@mariozechner` vs `@earendil-works` namespace: a real issue, but resolution depends on understanding which namespace pi currently exposes — requires investigation before a plan can be written.
- Improve skill files untracked: trivial one-line fix (`git add agent/skills/improve/`); didn't warrant a full plan.
