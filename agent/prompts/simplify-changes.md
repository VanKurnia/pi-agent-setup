---
description: simplifying and dedup recent changes.
---

Use `git_diff` and `detect_changes` to review all of our changes, and check if there are anything that could be simplify or dedup to enforce DRY and current workspace rules, structure, and already existing mechanism.